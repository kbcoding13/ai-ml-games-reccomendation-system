# Copy to .env and adjust after running the full training pipeline
# (training/download_data.py -> build_content_model.py -> build_collab_model.py
# -> build_hybrid_index.py) to switch from the demo dataset to the
# full-scale trained model.

GAMES_PATH=data/processed/games.parquet
NEIGHBORS_PATH=data/models/hybrid_neighbors.json
DEFAULT_TOP_K=6
